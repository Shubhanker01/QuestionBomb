export interface IMock {
    mockTitle: string,
    noOfQuestions: number,
    subjectId: number,
    duration: number,
    difficultyLevel: string,
    section: string,
    subsection: string,
    description: string
}